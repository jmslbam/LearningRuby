<% content_for :head do %>
  Page - <%= @page.title %>
<% end %>

<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @page.title %>
</p>

<p>
  <b>Body:</b>
  <%= @page.body %>
</p>


<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>

<div class="sidebar">
  <%= render :partial => "links/widget", :locals => { :links => @links } %>
</div>
